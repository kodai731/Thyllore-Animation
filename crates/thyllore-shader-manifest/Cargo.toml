[package]
name = "thyllore-shader-manifest"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"
authors = ["kodai731"]
description = "Parses shaders/passes.toml, validates it against the shader sources, and generates the pass manifest for ThylloreAnimation. Usable from build scripts"
repository = "https://github.com/kodai731/Thyllore-Animation"

[dependencies]
thiserror = "1"
thyllore-spirv-reflect = { path = "../thyllore-spirv-reflect" }
toml = "0.8"

[package.metadata.slang]
version = "2026.18"
