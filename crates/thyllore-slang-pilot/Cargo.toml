[package]
name = "thyllore-slang-pilot"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"
authors = ["kodai731"]
description = "Pilot: the volume_shell Slang module compiled to C++ by slangc and called from Rust, checked against the hand-written Rust mirror"
repository = "https://github.com/kodai731/Thyllore-Animation"

[dependencies]
cgmath = "0.18"
thyllore-effect-core = { path = "../thyllore-effect-core" }

[build-dependencies]
cc = "1.2"

[dev-dependencies]
fastrand = "2"
