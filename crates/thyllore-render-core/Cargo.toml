[package]
name = "thyllore-render-core"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"
authors = ["kodai731"]
description = "Layer 3 abstract render backend for ThylloreAnimation — pure GPU handle types and UBO structs. No ECS or Vulkan dependency"
repository = "https://github.com/kodai731/Thyllore-Animation"

[dependencies]
anyhow = "1"
cgmath = "0.18"
serde = { version = "1", features = ["derive"] }
thyllore-anim-core = { path = "../thyllore-anim-core" }
thyllore-model-core = { path = "../thyllore-model-core" }
thyllore-scene-core = { path = "../thyllore-scene-core" }
thyllore-spirv-reflect = { path = "../thyllore-spirv-reflect" }

[dev-dependencies]
ron = "0.8"
